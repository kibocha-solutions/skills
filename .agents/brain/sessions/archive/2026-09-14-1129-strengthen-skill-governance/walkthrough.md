# Walkthrough: strengthen skill governance

## Context

The previous remediation made the skill packages mechanically compliant, but
the user found that the authority language remained too soft and that the
human-facing authoring standard belongs inside the skill-creation package.
This pass starts from the exact former PBO rules and tests each rule for a
universal form before changing shared governance.

## Progress

- Archived the completed `official-skills-reconciliation` session after the
  new task arrived.
- Opened this session before substantive governance edits.
- Recovered the 453-line PBO source from the mounted-volume Trash and read it
  from start to finish.
- Classified all 24 source rules in `meta/rules-universalization.md` before
  editing shared governance.
- Replaced the authority ladder with direct command language and a narrow
  direct, unambiguous, realistic law-or-harm exception.
- Made the second section of `AGENTS.md` a numbered skill discovery, full-read,
  checklist, phase-reread, resumption-reread, and final-verification procedure.
- Added the qualifying dependency-installation gate and a detailed
  `system-init` reference covering need, provenance, scope, execution,
  privilege failure, and verification.
- Removed conflicting permission-before-installation rules from the document,
  PDF, presentation, spreadsheet, graph, diagram, and Writerside procedures.
- Moved the authoring standard from `docs/` to
  `skill-creator/references/skill-authoring-standard.md` and updated every
  active route.
- Removed duplicated proposal typography from the general donor-narrative
  reference. Exact proposal defaults now remain only in the PBO-specific
  reference.
- Added an absolute CI/CD AI-attribution prohibition covering commits, author
  fields, co-author trailers, pull requests, issues, reviews, tags, releases,
  changelogs, deployment records, generated-by fields, and metadata.
- Replaced all `AI Assistant` author defaults in the bundled DOCX and PPTX
  helpers with `Editor` and `ED`.
- Extended repository validation to enforce the `AGENTS.md` word ceiling,
  opening section order, authoring-standard location, obsolete installation
  gates, conditional AI-attribution exceptions, and AI author residue in
  Python sources.

## Verification

- Re-read every affected instruction file and governing reference from start
  to finish after the final edits.
- Confirmed `AGENTS.md` contains 1,487 words and opens with General commands
  followed immediately by Working with skills.
- Confirmed the universalization register contains exactly 24 rule entries.
- Confirmed the former `docs/SKILL_AUTHORING_GUIDE.md` is absent and the active
  routes resolve to `skill-creator/references/skill-authoring-standard.md`.
- Confirmed exact 12/12/14/16 point proposal formatting appears only in
  `documentation/references/pbo-document-rules.md`.
- Confirmed no active skill package contains `AI Assistant`, a conditional
  AI-attribution exception, or an obsolete separate-permission installation
  gate.
- Parsed nine modified Office helpers and verified that both DOCX and PPTX
  validation commands report `Editor` as the default author.
- Confirmed no test cache remains in the repository.
- Ran `tools/validate-all.sh`. All 21 skills passed structure, prose, link,
  JSON, Draw.io XML, Python, Bash, and Git whitespace checks.
- Corrected the stale Tier 2 memory pointer and added a superseded-path note to
  the older authoring-guide handoff.
- Did not propagate through bootstrap, commit, push, publish, or deploy.

## Completion

Every acceptance criterion passed. The session is `DONE` and remains active
until a new task triggers archival.
