# Tasks: Skills Deepening, References & Good vs Bad Examples

State: IN_PROGRESS

## Phase 1: Orchestration & Infrastructure Skills (Group A)
- [x] Deepen `maestro/`: step-by-step directives, `examples/` for session lifecycle & threshold recovery
- [x] Deepen `bootstrap/`: step-by-step commands, `examples/` for multi-host alignment & conflict handling
- [x] Deepen `skill-creator/`: `examples/` for benchmark creation & evaluation review
- [x] Deepen `system-design/`: `examples/` for clean vs anti-pattern system boundaries
- [x] Deepen `system-init/`: `examples/` for toolchain audit & safe installation gates
- [x] Deepen `ssh/`: `examples/` for SSH signing bridge & bastion configuration

## Phase 2: Document & Office Skills (Group B)
- [x] Deepen `docx/`: create `references/` and `examples/` for document generation
- [x] Deepen `pdf/`: create `references/` and `examples/` for form filling & validation
- [x] Deepen `pptx/`: create `references/` and `examples/` for slide deck architecture
- [x] Deepen `xlsx/`: create `references/` and `examples/` for financial recalculations
- [x] Deepen `doc-coauthoring/`: create `references/` and `examples/` for PRD interviews

## Phase 3: Integration, Frontend & Communication Skills (Group C)
- [x] Deepen `mcp-builder/`: create `references/` and `examples/` for Python/Node servers
- [x] Deepen `frontend-design/`: create `references/` and `examples/` for UI layout & styling
- [x] Deepen `webapp-testing/`: create `references/` and `examples/` for Playwright testing
- [x] Deepen `communications/`: create `references/` and `examples/` for external notices
- [x] Deepen `discernment-nudge/`: create `references/` and `examples/` for reflection sets

## Phase 4: Verification, Checklists & Packaging
- [x] Confirm pre-completion checklists on all 21 skills
- [x] Verify line counts (< 500 lines) and zero em dashes
- [x] Verify all linked references and examples exist
- [/] Await user signing resolution to produce single `/ci-cd` compliant commit

