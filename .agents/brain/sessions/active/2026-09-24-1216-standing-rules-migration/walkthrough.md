# Walkthrough: Standing Rules Migration

## Execution

- User approved the placement in the coverage map, publication by amend, push,
  and bootstrap, and removal of the msngi memory index entry for rule 20.
- Documentation skill: 2.7 (propose before changing locked wording), 3 (no
  `shall` in procedure steps or correspondence), 5.4 (unrequested financial
  amounts, compliance status, eligibility statement), 7.9 and checklist
  (title-only referencing scoped to external deliverables; relative links and
  no higher-tier reference in a documentation library). Writerside reference:
  cumulative trees, tier rule, and relative linking under Access and
  Sensitivity; validation removes an upward reference instead of converting it
  to plain text. Install reference routes to those rules.
- System-design skill: 5 (name reuse, recorded precedent research, plural and
  column-style names), 6.10 (change tests), 6.16 (illustrative examples);
  naming reference gains name examples and precedent-research rules.
- Evals: documentation case 5, system-design case 4.
- `tools/validate-all.sh`: PASS. Changed files read in full.
- msngi: `AGENTS.md` deleted, memory index entry removed; amended and pushed
  as 5d1261c.
- Paired evaluation runs not executed (independent agents not requested);
  cases are objective and ready for a later run.
